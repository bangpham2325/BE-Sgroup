extends ./main.pug

block content
    h3 Bai Cua Bang Dep Trai
        .mt-3
            .row
                each session in sessions
                    .card-body
                        h5.card-title #{session.user._id}
                        p.card-text #{session.log}
                        
                    
